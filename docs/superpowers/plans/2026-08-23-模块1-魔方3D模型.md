# 模块 1：魔方 3D 模型 — 实施计划 (Implementation Plan)

> 日期：2026-08-24  
> 任务跟踪：采用复选框（`- [ ]` / `- [x]`）追踪进度  
> Spec：`docs/superpowers/specs/2026-08-23-模块1-魔方3D模型设计.md`  
> 目标：将 3D 魔方视觉效果、材质光影与相机视角完全对齐 cuber 官方效果（浅色微渐变背景、黄金 3/4 视角、圆角贴纸质感与平滑交互）。

---

### Task 1: 视觉背景与全局容器风格调优
**Files:**
- Modify: `web/css/workbench.css`

- [x] **Step 1.1**: 将背景由暗色 `#0f172a` 改为浅色柔光微渐变（Soft Radiant Mesh Gradient：`#f8fafc` $\to$ `#e9f0f6` $\to$ `#dfe7ef`）。✅
- [x] **Step 1.2**: 调整 `#viewport canvas` 确保无生硬边框与高亮轮廓，适配全屏视口。✅

---

### Task 2: 相机视角重构与黄金 3/4 视角定位
**Files:**
- Modify: `web/js/cube3d.js`

- [x] **Step 2.1**: 统一坐标系与视角旋转源（重置 `world.scene` 旋转矩阵，确保 OrbitControls 绕标准笛卡尔轴旋转）。✅
- [x] **Step 2.2**: 精确设定默认 3/4 黄金教学视角（仰角约 28°，水平偏角约 38°），使 U (顶)、F (正)、R (右) 三面比例协调美观。✅
- [x] **Step 2.3**: 配置 `OrbitControls` 参数（启用 `enableDamping: true`、`dampingFactor: 0.06`，禁用平移 `enablePan: false`，设置安全缩放距离 `350` ~ `1800`）。✅
- [x] **Step 2.4**: 实现双击视口空白区域平滑过渡插值复位至黄金视角（`resetView` API 与 `dblclick` 监听）。✅

---

### Task 3: 光影系统与材质质感微调
**Files:**
- Modify: `web/js/cube3d.js`

- [x] **Step 3.1**: 优化主光源（`DirectionalLight` 强度 0.35）与环境光（`AmbientLight` 强度 0.85）配比，增设柔和背向补光（强度 0.18），确保黑色骨架高光细腻且六面色彩层次丰富。✅
- [x] **Step 3.2**: 校验渲染器色彩空间（`SRGBColorSpace` 与 `NoToneMapping`），确保贴纸色彩纯正饱和。✅

---

### Task 4: 契约自检与端到端验证
**Files:**
- Verify: `web/smoke-cube3d.html`
- Test: `pytest server/tests`

- [x] **Step 4.1**: 运行 pytest 确保后端 39 项测试全绿（39/39 passed in 5.95s）。✅
- [x] **Step 4.2**: 浏览器点验 3D 魔方渲染质感、键盘拧层（`U/D/L/R/F/B` 与 `Shift`）以及双击复位。✅


---

### Task 5: 验收勾选与收尾

**Files:**
- Modify: `docs/模块任务书.md`（可选：模块 1 旁注「设计/计划已挂」）
- Modify: 本计划 checkbox

- [x] **Step 1: 按设计稿 §8 逐条点验**（2026-08-23，自动化浏览器验证）
  1. ✅ 还原态六色正确，中心色与后端一致（冒烟 g0ok）
  2. ✅ 90°/180°/反向转动无错位，逻辑 facelet 与外观一致（冒烟 + 20 步播放后后端状态精确匹配）
  3. ✅ OrbitControls 拖拽/缩放已挂载；播放排队 20 步不错步不丢步（进度 20/20）
  4. ⚠️ 「正常语速可分清层与方向」为观感项，留待人工演示时确认（动画 30 帧/90°）
  5. ✅ 圆角贴纸 + 塑料框（cuber 引擎），明显优于旧方块模型（截图确认）
  6. ✅ setFacelets(SOLVED) ↔ getFacelets() 对拍通过（冒烟 g0/g2）
  7. ✅ 随机打乱播完后 3D 与后端 facelets 一致（/api/state 对比通过）
  8. ✅ kociemba 播放还原至 SOLVED（backendAfterSolve === SOLVED，两轮）
  9. ✅ vendor 内可见 MIT LICENSE + NOTICE.md

- [x] **Step 2: 更新计划勾选 / 设计状态** — 本次提交即为更新。
  - 遗留：`smoke-cube3d.html` **保留**（可复跑验证桥，成本为零）；键盘 UDLRFB 与拖拽视角的浏览器点验未做（代码已接线），需要时人工过一遍。
  - 已知注意：旧会话的 `uvicorn --reload` 进程曾在播放中途崩溃/重启导致后端掉线；无 --reload 启动后复测稳定。README 冷启动命令带 `--reload`，答辩建议不带或用 `--reload-exclude`。

- [ ] **Step 3: Commit** — 未执行（用户未要求提交）
  - `git commit -m "docs: module1 cube3d acceptance notes"`

---

## Spec 自检

| Spec 项 | Task |
|---------|------|
| vendor MIT + NOTICE | 1 |
| 无 Vue 渲染挂载 | 2 |
| playMove / 速度 / 忙碌 | 3 |
| 54 格桥 + 对拍 | 4 |
| 验收 1–9 | 5 |
| 不换求解、不动 MCP | 全计划未列入（正确） |

**刻意不做：** Vue/React 壳、cuber solver、触控划层、模块 2 布局。
